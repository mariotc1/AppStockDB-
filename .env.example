# ── Base de datos (solo la API la necesita, corre en Docker) ──────────────────
DB_ROOT_PASSWORD=rootpassword
DB_HOST=db
DB_PORT=3306
DB_USER=appuser
DB_PASSWORD=apppassword
DB_NAME=db_inmuebles

# ── API (la app de escritorio usa esta URL) ────────────────────────────────────
# Desarrollo local (Docker):  http://127.0.0.1:5000
# Producción (servidor):      https://tu-api.tu-dominio.com
API_BASE_URL=http://127.0.0.1:5001
